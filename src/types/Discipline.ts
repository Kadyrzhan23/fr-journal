export interface IDiscipline {
    _id: string;
    name: string;          // Kimyo fanlari
    code: string;          // 03.00.00
    slug: string;          // kimyo-fanlari
    description?: string;
    createdAt: Date;
}